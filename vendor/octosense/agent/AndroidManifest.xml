<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.makepad.octosense.agent"
    android:versionCode="1"
    android:versionName="0.1">

    <!-- Only OctoSense's own platform-signed apps may bind. -->
    <permission android:name="dev.makepad.octosense.permission.BIND_AGENT_PLATFORM"
        android:protectionLevel="signature" />
    <!-- The role-owned SystemUI sensor adapter uses the same signature boundary. -->
    <uses-permission android:name="dev.makepad.octosense.permission.BIND_AGENT_PLATFORM" />

    <!-- tasks -->
    <uses-permission android:name="android.permission.REAL_GET_TASKS" />
    <uses-permission android:name="android.permission.MANAGE_ACTIVITY_TASKS" />
    <uses-permission android:name="android.permission.START_TASKS_FROM_RECENTS" />
    <uses-permission android:name="android.permission.REMOVE_TASKS" />
    <uses-permission android:name="android.permission.STOP_APP_SWITCHES" />
    <uses-permission android:name="android.permission.FORCE_STOP_PACKAGES" />
    <!-- screen -->
    <uses-permission android:name="android.permission.ACCESS_SURFACE_FLINGER" />
    <uses-permission android:name="android.permission.READ_FRAME_BUFFER" />
    <uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" />
    <!-- input -->
    <uses-permission android:name="android.permission.INJECT_EVENTS" />
    <!-- settings -->
    <uses-permission android:name="android.permission.MANAGE_TIME_AND_ZONE_DETECTION" />
    <uses-permission android:name="android.permission.SUGGEST_MANUAL_TIME_AND_ZONE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.CONTROL_DISPLAY_COLOR_TRANSFORMS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.NETWORK_SETTINGS" />
    <uses-permission android:name="android.permission.MANAGE_NETWORK_POLICY" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_PRIVILEGED" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.MODIFY_PHONE_STATE" />
    <uses-permission android:name="android.permission.DEVICE_POWER" />
    <!-- notifications -->
    <uses-permission android:name="android.permission.MANAGE_NOTIFICATIONS" />
    <!-- apps -->
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" />
    <!-- statusbar -->
    <uses-permission android:name="android.permission.STATUS_BAR" />
    <uses-permission android:name="android.permission.STATUS_BAR_SERVICE" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- update: fetch releases, install the Home app, restart into the new slot -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REBOOT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries><package android:name="com.android.systemui" /></queries>
    <application
        android:name=".AgentApplication"
        android:label="OctoSense Agent"
        android:persistent="true"
        android:directBootAware="true"
        android:defaultToDeviceProtectedStorage="true"
        android:allowBackup="false"
        android:supportsRtl="true">

        <service android:name=".AgentPlatformService"
            android:exported="true"
            android:directBootAware="true"
            android:permission="dev.makepad.octosense.permission.BIND_AGENT_PLATFORM">
            <intent-filter>
                <action android:name="dev.makepad.octosense.agent.AGENT_PLATFORM" />
            </intent-filter>
        </service>

        <service android:name=".UpdateJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <receiver android:name=".UpdateActionReceiver" android:exported="false" />

        <!-- First-boot provisioning: the OctoSense palette and the shell's defaults. -->
        <receiver android:name=".ProvisionReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
